from django.apps import AppConfig


class TimelineConfig(AppConfig):
    """AppConfig for the timeline app (P2-08)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.timeline"
