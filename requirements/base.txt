django==4.2.11
python-dotenv==1.0.1
djangorestframework==3.15.2
django-autoslug==1.9.9
django-countries==7.5.1
django-phonenumber-field==7.3.0
phonenumbers==8.13.32
drf-yasg==1.21.7
# P3-fix2 / openapi-typegen: drf-yasg は APIView (非 ViewSet) で action_map=None
# を踏んで落ちるため、drf-spectacular を併用して OpenAPI 3.0 schema を吐く
# (frontend openapi-typescript codegen 用)。drf-yasg は /redoc/ の人間向け UI
# 互換のために残置。
drf-spectacular==0.29.0
Pillow==10.2.0
argon2-cffi==23.1.0
djoser==2.2.2
django-taggit==5.0.1
django-filter==24.1
django-celery-email==3.0.0
cloudinary==1.39.1
django-celery-beat==2.6.0
redis==5.0.3
django-redis==5.4.0
celery==5.4.0
flower==2.0.1
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1

# --- Added in P0-01 (Phase 0: 基盤整備) ---
# Version policy: 2026-04 時点での latest stable を基準。python-reviewer 指摘反映。

# WebSocket / リアルタイム配信 (Phase 3 DM で使用)
channels==4.3.2
channels-redis==4.3.0
daphne==4.1.2

# 認証・ソーシャルログイン (Phase 1 Google OAuth)
# 5.8.0 は Django>=5.1 を要求するため、Django 4.2 と互換な最終版 5.4.3 を pin。
# Django 5 に上げる時 (Phase 9 以降) に最新メジャーへ移行。
social-auth-app-django==5.4.3

# 決済 (Phase 8 プレミアム)
# TODO(Phase 8): 実装着手時に最新メジャー (15.x 系) への移行を検討。API 破壊的変更あり。
stripe==10.12.0

# AWS / S3 ストレージ (Phase 0.5, 1+ でメディア配信)
boto3==1.42.93
django-storages==1.14.4

# Markdown レンダリング + XSS サニタイズ (Phase 1 ツイート, Phase 6 記事)
bleach==6.3.0
markdown2==2.5.0
Pygments==2.18.0

# OGP カード自動取得 (P2-07): SSRF 対策で TCP 接続後の peer IP 検証を行うため、
# httpx の HTTPTransport をサブクラス化して socket レイヤをフックする。
# beautifulsoup4 は <meta property="og:..."> をパースするだけなので軽量で十分。
httpx==0.27.0
beautifulsoup4==4.12.3

# スラッグ生成 (Phase 6 記事)
python-slugify==8.0.4

# RSS フィード取得 (Phase 7 Bot)
feedparser==6.0.11

# AI API (Phase 7 Bot 要約、Phase 8 記事下書き AI、 Phase 13 自動翻訳)
# TODO(Phase 7/8): SDK は頻繁にリリースされるため、実装着手時に最新安定版へバンプする。
openai==1.42.0
anthropic==0.34.1

# Phase 13: ツイート本文の言語自動検出 (langdetect は Google CLD2 port、 BSD、 offline)
langdetect==1.0.9

# GitHub API (Phase 6 記事連携 push)
PyGithub==2.4.0

# エラートラッキング (Phase 0 観測性)
sentry-sdk[django]==2.58.0

# 構造化ログ (Phase 0 観測性)
structlog==24.4.0

# レート制限 (Phase 1 スパム検知階層化)
django-ratelimit==4.1.0

# GitHub OAuth トークン暗号化 (Phase 6 記事連携)
# CRITICAL: 43.x 系は RSA/X.509 関連 CVE 修正未適用のため 44.x 以上必須
cryptography==44.0.3
