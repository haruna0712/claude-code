-r base.txt

psycopg2-binary==2.9.9
watchfiles==0.21.0
black==24.3.0

# --- テスト基盤 (P1-21 で本配線) ---
# pytest: base.txt には無いのでここで pin。8.3.x 系は Python 3.12 サポート済み。
pytest==8.3.3
# Django との統合 (settings 自動読み込み / django_db / reuse-db 等)
pytest-django==4.9.0
# カバレッジ計測 (coverage.py の pytest プラグイン)
pytest-cov==5.0.0
# mocker fixture (unittest.mock を pytest style で使う)
pytest-mock==3.14.0
# factory-boy: DB fixture 生成の標準
factory-boy==3.3.1
# freezegun: 時刻固定テスト用
freezegun==1.5.1
# pytest-asyncio: Channels の WebsocketCommunicator は async API なので必要 (P3-02)。
# Channels 4.x + pytest-asyncio 0.24.x の組み合わせで `@pytest.mark.asyncio` が動く。
pytest-asyncio==0.24.0

# --- Lint / format gate (CI parity) ---
# pre-commit: ローカル commit 時に CI の pre-commit job と同等の検査を走らせる。
# CI workflow が `pip install pre-commit==3.8.0` で固定しているため同バージョンを pin。
# devcontainer 起動時に setup-project.sh が `pre-commit install` を呼び .git/hooks/pre-commit
# にフックを登録する。
pre-commit==3.8.0
